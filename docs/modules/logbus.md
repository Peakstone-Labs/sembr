# logbus

> **Status**: pending review

In-process structured log event bus. Captures Python `logging` records, routes them through an async queue, and fans out to SSE subscribers (dashboard live log stream). Zero external dependencies — pure asyncio.

<!-- Review and fill in this page before opening the module to contributors. -->
