# dashboard

> **Status**: pending review

Read-model aggregation and monitoring UI. Provides JSON endpoints at `/api/dashboard/*` and serves a plain HTML + Alpine.js + Chart.js frontend at `/dashboard`. Covers per-feed fetch outcomes, embedder stats, and article pipeline counts. Requires `DASHBOARD_TOKEN` when exposed beyond localhost.

<!-- Review and fill in this page before opening the module to contributors. -->
